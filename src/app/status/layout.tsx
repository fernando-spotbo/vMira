export const metadata = {
  title: "Mira Status — System Health",
  description: "Real-time status of Mira AI services. Check if Mira Chat, API, Code, and Model Inference are operational.",
};

export default function StatusLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
